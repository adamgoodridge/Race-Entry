## Progress Log

### Task 1 — Create core entities
**Status:** Done
**Commit:** feat(task-1): create all 6 core entities and enums

Created the following anemic JPA entities and enums per PRD section 2:
- `owner/Owner.java` — ownerId, name, contactEmail
- `boat/Boat.java` — boatId, ownerId, name, boatClass
- `event/Event.java` — eventId, name, date (LocalDate), location
- `driver/Driver.java` — driverId, name, licenseNumber
- `entry/Entry.java` — entryId, boatId, eventId, status (EntryStatus)
- `entry/EntryStatus.java` — PENDING, ACTIVE, CANCELLED
- `entry/EntryDriverMap.java` — mapId, entryId, driverId, role (DriverRole)
- `entry/DriverRole.java` — HELMSMAN, CREW

All entities store ID references only — no object references between entities. No business logic in entities.

---

### Task 2 — Create repositories
**Status:** Done
**Commit:** feat(task-2): create all repositories per PRD spec

- `owner/OwnerRepository.java` — extends JpaRepository<Owner, Long>
- `boat/BoatRepository.java` — findByOwnerId(Long)
- `event/EventRepository.java` — extends JpaRepository<Event, Long>
- `driver/DriverRepository.java` — extends JpaRepository<Driver, Long>
- `entry/EntryRepository.java` — findByBoatId(Long), findByEventId(Long)
- `entry/EntryDriverRepository.java` — findDriverIdsByEntryId (JPQL), findEntryIdsByDriverId (JPQL)

JPQL queries on EntryDriverRepository project to Long (ID-only) to keep the layer anemic.

---

### Task 3 — Create services
**Status:** Done
**Commit:** feat(task-3): create four services and BoatStatus enum

Supporting changes:
- `boat/BoatStatus.java` — ACTIVE, ORPHANED enum (required by cascade rules)
- `boat/Boat.java` — added status field (default ACTIVE)
- `entry/EntryDriverRepository.java` — added deleteByEntryIdAndDriverId JPQL

Services:
- `owner/OwnerService.java` — create, findById, delete
- `boat/BoatService.java` — register (validates owner exists), findById, findByOwner, markOrphaned
- `entry/EntryService.java` — create (enforces one-per-boat-per-event), findById, findByEvent, findByBoat, updateStatus (enforces min driver before ACTIVE), cancel (sets CANCELLED + deletes EntryDriverMaps), assignDriver, removeDriver (enforces min one driver)
- `driver/DriverService.java` — create (validates license not blank), findById

---

### Next task
Task 4 — Create RacingEventMediator:
- Orchestrates owner deletion cascade: delete owner → orphan boats → cancel active entries (which removes EntryDriverMaps)
- No business logic in the mediator itself — calls services and repositories in sequence
