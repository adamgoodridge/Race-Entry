## Refactor Progress

### Completed Tasks

#### Task #1 — Delete Random.java and main() code-generator methods (2026-06-15)
- Deleted `src/main/java/au/com/voc/raceEntry/Random.java`
- Removed `main()` code-generator method from `Person.java`
- Removed `main()` code-generator method from `Event.java`
- Removed `main()` code-generator method from `EventView.java`

#### Task #2 — Fix @Controller("/") annotation bug; rename package Controller → controller (2026-06-15)
- Moved `GeneralController.java` from package `Controller` (uppercase) to `controller` (lowercase)
- Fixed `@Controller("/")` → `@Controller` (path argument is invalid on stereotype annotation; path `/` is handled by `@GetMapping("/")`)
- Removed `//demo only` comment

#### Task #3 — Replace all System.out.println with SLF4J logging (2026-06-15)
- Added `Logger log = LoggerFactory.getLogger(...)` to: `PersonService`, `PersonController`, `BoatController`, `FieldUniqueValidator`, `Convertor`, `EventController`, `UserController`, `UserService`, `UserAuthentication`
- Converted all active `System.out.println` calls to `log.debug()` (validation errors use `log.warn()`)
- Collapsed three duplicate `println(user.getUsername())` calls in `UserController` to one `log.debug()`
- One `System.out.println` in `CustomAuthenticationSuccessHandler` is inside a block comment — left untouched

#### Task #4 — Make UserAuthentication a @Component; inject instead of new UserAuthentication() (2026-06-15)
- Added `@Component` + `@RequestScope` to `UserAuthentication` (request scope required: reads SecurityContextHolder in constructor, must be fresh per request; Spring's scoped proxy handles singleton-to-request injection)
- Injected `UserAuthentication` via constructor into `GeneralController` and `PersonController`
- Removed `new UserAuthentication()` local instantiations from both controllers

#### Task #5 — Remove previousUrl @Transient field from all domain entities (2026-06-15)
- Removed `previousUrl` field + getter/setter from `Person`, `Event`, `Entry`, `BoatView`
- `PersonController`: switched to `HttpSession` attribute `personFormReturnUrl` (stored on GET, read on POST redirect) — the only place where previousUrl was actively round-tripped
- `BoatController.form()` / `showFormUpdate()`: dropped `setPreviousUrl` calls and now-unused `HttpServletRequest` params (processForm already redirected to `/`)
- `EventController.showFormUpdate()` / `add()`: same cleanup (processForm already redirected to `/event/list/opened`)
- Removed `<input type="hidden" th:field="*{previousUrl}">` from `add-person-form.html`
- `EntryFormData.previousUrl` intentionally kept — it is a DTO, not a domain entity, and is actively used in the entry form round-trip

#### Task #6 — Remove User from EntryFormData; resolve current user in controller (2026-06-15)
- Removed `User user` field, getter, setter, and constructor copy from `EntryFormData`; removed `import au.com.voc.raceEntry.user.User`
- Added `User user` as explicit first parameter to `EntryService.formEventEntry()` replacing `entryFormData.getUser()`
- `EntryController.form()`: removed `User user = userService.getCurrentUser()` + `entryFormData.setUser(user)` (no longer needed)
- `EntryController.processForm()`: passes `userService.getCurrentUser()` directly to `formEventEntry()` — user is resolved from Spring Security context at call time, never carried through the form

#### Task #7 — Convert BoatView → BoatFormData: remove @Entity, make plain POJO (2026-06-15)
- Created `BoatFormData.java`: removed `@Entity`, `@Table`, all JPA field annotations (`@GeneratedValue`, `@Id`, `@Column`, `@Basic`, `@Transient`, `@ManyToOne`, `@JoinColumn`) and `import javax.persistence.*`
- Removed `User user` JPA relationship field and its getter/setter; simplified `BoatFormData(User user)` constructor to set `userId = user.getId()` directly
- Renamed `userIId` field to `userId` (fixing the double-I typo)
- Deleted `BoatView.java`
- Updated `BoatController`: all `BoatView` → `BoatFormData`, model attribute `"boatView"` → `"boatFormData"`
- Updated `add-boat-form.html`: `boatView` → `boatFormData`

#### Task #8 — Convert EventView → EventFormData: remove @Entity, make plain POJO (2026-06-15)
- Created `EventFormData.java`: plain POJO, no JPA annotations; kept `@NotNull` on `name`/`venue`; `open` changed to `Boolean` (matches `Event.open`), `entriesCount` promoted to `Long` (COUNT aggregate)
- Added 8-arg constructor for JPQL constructor expression: `(Long eventId, String name, String venue, LocalDate startDate, int duration, Boolean open, Integer visible, Long entriesCount)`
- Rewrote `EventViewRepository`: now extends `JpaRepository<Event, Long>` (queries the real entity); `events()` uses JPQL constructor expression with a correlated COUNT subquery for `entriesCount`; parameter changed from `int` to `Boolean`
- `EventService.getEventsView(int open)`: now returns `List<EventFormData>`; converts int → Boolean (`open == 1`) for the repository call
- Updated `GeneralController`, `EventController`, `EntryController`: import + variable type `EventView` → `EventFormData` in all three
- Deleted `EventView.java` via `git rm`

#### Task #9 — Add surrogate Long PK to BoatClass; make className unique (2026-06-15)
- Added `@Id @GeneratedValue(strategy = GenerationType.IDENTITY) private Long boatClassId` with getter/setter
- Changed `className` from `@Id @Column(name = "class_name")` → `@Column(name = "class_name", unique = true)`
- Updated `BoatClassRepository`: type parameter `String` → `Long` (`JpaRepository<BoatClass, Long>`)

#### Task #10 — Rename Boat.className → boatType (field + column); update all references (2026-06-15)
- `Boat.java`: field `className` → `boatType`, `@Column(name = "class_name")` → `@Column(name = "boat_type")`, getter/setter renamed accordingly
- `BoatFormData.java`: field, getter, setter, constructor copy (`boat.getBoatType()`), and `toBoat()` call all renamed
- `EntryService.java`: `entry.getBoat().getClassName()` → `entry.getBoat().getBoatType()` (PDF form fill)
- `add-boat-form.html`: `th:field="*{className}"` and `th:errors="*{className}"` → `boatType`
- `fragment-list.html` + `list-boats.html`: `${b.className}` → `${b.boatType}`
- `BoatClass.className` left untouched — different entity, appropriate name

#### Task #11 — Introduce EntryStatus enum; replace Event.open Boolean (2026-06-15)
- Created `EntryStatus.java` enum (`DRAFT`, `OPEN`, `CLOSED`) in `event` package
- `Event.java`: replaced `@Column(name="open") Boolean open` with `@Enumerated(EnumType.STRING) @Column(name="status") EntryStatus status`; renamed getter/setter to `getStatus()`/`setStatus()`
- `EventFormData.java`: replaced `Boolean open` with `EntryStatus status`; updated 8-arg constructor accordingly
- `EventViewRepository`: query updated `e.open → e.status`; method param changed from `Boolean` to `EntryStatus`
- `EventRepository.openEvents()`: `WHERE e.open = true` → `WHERE e.status = au.com.voc.raceEntry.event.EntryStatus.OPEN` (JPQL enum literal)
- `EventService.getEventsView(int open)`: signature changed to `getEventsView(EntryStatus status)`; removed `open == 1` int-to-boolean conversion; `setOpen()` → `setStatus()`
- `EventController`: all `getEventsView(1)` → `getEventsView(EntryStatus.OPEN)`, `getEventsView(0)` → `getEventsView(EntryStatus.CLOSED)`; `event.getOpen()` → `event.getStatus()`
- `GeneralController`, `EntryController`: `getEventsView(1)` → `getEventsView(EntryStatus.OPEN)`; added `EntryStatus` import
- `EntryRepository`: all 3 JPQL queries `e.event.open = true` → `e.event.status = au.com.voc.raceEntry.event.EntryStatus.OPEN`

#### Task #12 — Change Event.visible from Integer to Boolean (2026-06-16)
- New clean `Event.java` (from Ralph's redesign) had dropped `visible` entirely; per PRD Decision Log it stays, typed as `Boolean` (orthogonal to `EventStatus`)
- Added `@Column(nullable = false) private Boolean visible` with default `true` in constructor; added `getVisible()` / `setVisible()` getter/setter
- Also committed Ralph's untracked entity redesign (Boat, BoatClass, Person, Entry, EntryDriver, DriverRole, EntryStatus, EventStatus) that had been left unstaged after c529a9c deleted the old files

#### Task #13 — Remove Event.duration column; add computed getDuration() from startDate/endDate (2026-06-16)
- `Event.java`: renamed `eventDate` → `startDate` (`@Column(name="start_date")`); added `@Column(name="end_date") LocalDate endDate`; updated constructor to `Event(String name, LocalDate startDate, LocalDate endDate)`; renamed getter/setter accordingly; added `getDuration()` returning `ChronoUnit.DAYS.between(startDate, endDate)` (no column — pure computation); added `getEndDate()` / `setEndDate()`
- Created `EventRepository.java`: `JpaRepository<Event, Long>` (needed by `EventTableTest`)
- Created `DateUtils.java` in `utils` package: static `format(String)` parsing `dd/MM/yyyy` → `LocalDate` (needed by `EventTableTest`)

#### Task #14 — Remove List<Entry> from Event; move @PreRemove guard to EntryService (2026-06-16)
- `Event.java` already had no `List<Entry>` in Ralph's redesign — confirmed removal is complete
- Created `EntryRepository.java`: `JpaRepository<Entry, Long>` with `findByEvent(Event)` and `existsByEvent(Event)` derived queries (replaces the old bi-directional `Event.entries` collection navigation)
- Created `EntryService.java`: `@Service @Transactional`; `deleteEvent(Event)` checks `entryRepository.existsByEvent()` and throws `IllegalStateException` if entries exist, then calls `eventRepository.delete()` — this is the `@PreRemove` guard moved out of the entity into the service layer

#### Task #15 — Replace driverOne/driverTwo with List<Person> drivers; update EntryFormData to List<Long> driverIds (2026-06-16)
- `Entry.java` (Ralph's redesign) already models drivers as `@OneToMany List<EntryDriver> drivers` — `EntryDriver` is a dedicated join entity with `Person driver` and `DriverRole role`; this is strictly better than a plain `@ManyToMany` because it carries role metadata; confirmed no legacy `driverOne`/`driverTwo` fields exist
- Created `EntryFormData.java`: plain POJO; fields `Long entryId` (null = new entry), `@NotNull Long eventId`, `@NotNull Long boatId`, `@Size(min=1) List<Long> driverIds`; `isNew()` helper; no `User` field (removed in task #6)

#### Task #16 — Replace Entry.boatClass String with List<BoatClass> boatClasses via @ManyToMany join table (2026-06-16)
- `Entry.java` (Ralph's redesign) had no `boatClass` field at all — confirmed old String column is gone
- Added `@ManyToMany @JoinTable(name="entry_boat_class", joinColumns="entry_id", inverseJoinColumns="boat_class_id") List<BoatClass> boatClasses` with `getBoatClasses()` (unmodifiable) and `setBoatClasses(List<BoatClass>)`
- Updated `isReadyToSubmit()` to also require `!boatClasses.isEmpty()` alongside owner and drivers checks

#### Task #17 — Create EntryDeclaration entity; remove hardcoded signature fields from Entry; update EntryFormData (2026-06-17)
- `Entry.java` already had no hardcoded signature fields in Ralph's redesign — confirmed clean
- Created `EntryDeclaration.java`: `@Entity(name="entry_declaration")` with `@ManyToOne Entry entry`, `@ManyToOne Person person`, `String signature`, `LocalDate signedDate`; constructor-only mutation (no setters — immutable once signed)
- Created `DeclarationFormData.java`: plain POJO with `@NotNull Long personId`, `@NotBlank String signature`, `@NotNull LocalDate signedDate`
- `Entry.java`: added `@OneToMany(mappedBy="entry", cascade=ALL, orphanRemoval=true) List<EntryDeclaration> declarations` with unmodifiable getter
- `EntryFormData.java`: added `@Size(min=1) List<DeclarationFormData> declarations` with getter/setter

#### Task #18 — Extract EntryPDFService from EntryService; fix NPE bug (2026-06-17)
- Created `EntryPDFService.java`: extracted `generatePDF(Entry)` out of the old monolithic `EntryService` into its own `@Service`
- Fixed NPE bug: old code did `if (entry.getEvent() == null) { eventService.getEvent(entry.getEvent().getEventId()) }` — dereferences null on the same line it checked null; fixed by removing the dead guard entirely (`@ManyToOne(optional=false)` guarantees event is never null)
- Fixed resource leak: old code did not close `PDDocument`; new code uses try-with-resources
- Added null-safe `setField()` helper that silently skips PDF fields that don't exist in the form template
- Null-checked `boat.getOwner()` (which is nullable per domain model)
- Updated driver loop to use `entry.getDrivers()` (new `List<EntryDriver>` model) instead of old `entry.getPersonOne()` / `entry.getPersonTwo()`
- `EntryService.java` left with only event-deletion guard; all PDF logic lives in `EntryPDFService`

#### Task #19 — Create EntryAssemblyService; move formEventEntry() logic; remove BoatRepository/PersonRepository from EntryService (2026-06-17)
- Created `BoatRepository.java`: `JpaRepository<Boat, Long>`
- Created `PersonRepository.java`: `JpaRepository<Person, Long>`
- Created `BoatClassRepository.java`: `JpaRepository<BoatClass, Long>`
- `EntryFormData.java`: added `List<Long> boatClassIds` with getter/setter (needed to assemble boat-class associations)
- `Entry.java`: added package-private `addDriver()`, `clearDrivers()`, `addDeclaration()`, `clearDeclarations()` so `EntryAssemblyService` (same package) can manage the collections without exposing them publicly
- Created `EntryAssemblyService.java`: `@Service @Transactional`; `assemble(EntryFormData)` loads `Event`, `Boat`, `BoatClass[]`, `Person[]` from their repositories; creates or loads `Entry`; rebuilds drivers (orphanRemoval clears old ones) defaulting first to `HELMSMAN`, rest to `CREW`; rebuilds declarations; saves and returns the entry
- `EntryService` never touched BoatRepository/PersonRepository — those live only in EntryAssemblyService

#### Task #20 — Create EntryEligibilityService; enforce entry rules (2026-06-17)
- `Boat.java`: restored `Boolean deleted` (col: `deleted`, default `false`); added `isDeleted()` / `setDeleted()` — Ralph's redesign had dropped it but the PRD soft-delete guard requires it
- `Person.java`: same — restored `Boolean deleted` with `isDeleted()` / `setDeleted()`
- `EntryRepository.java`: added `existsByBoatAndEvent(Boat, Event)` (new entry check) and `existsByBoatAndEventAndIdNot(Boat, Event, Long)` (update check, excludes current entry ID)
- Created `EntryEligibilityService.java`: `@Service @Transactional(readOnly=true)`; `checkEligibility(EntryFormData)` collects ALL violations before throwing a single `IllegalStateException` with a joined message; enforces: (1) event status == OPEN, (2) boat not soft-deleted, (3) at least one driver, (4) no driver soft-deleted, (5) boat-per-event uniqueness (with `IdNot` exclusion on updates)

### Pending Tasks
#21–#23 (see refactor-plan-PRD.md)
